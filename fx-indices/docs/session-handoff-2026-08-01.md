# Хэндофф сессии — 01.08.2026

**Единственная точка входа в следующую сессию.** Всё остальное — по ссылкам внизу.

---

## ✅ ЗАКРЫТО 01.08.2026 в 00:40 МСК — свап алертов завершён

Старый алерт `5261914051` удалён пользователем вручную. На FX:EURUSD M15 остался
**единственный** алерт `5272544462` (v25, ver 2.0, `maxHoldH=42`), активен, с вебхуком.

Эталон вотчдога переснят: **6/6, 0 расхождений**. В эталоне EURUSD теперь
`5272544462` / pine `USER;bfab6682…` / ver 2.0 / 65 инпутов,
`maxBos=1 · stopMode=skip · keepSetup=false · poiMode=snapshot · maxHoldH=42`.

⚠️ **Правка в `watchdog/watchdog.mjs`:** манифест `STREAMS` жёстко задаёт `alert_id`, и
`--baseline` ищет потоки только по ним — с первого раза снялось 5/6, EURUSD выпал.
Строка 58 обновлена на новый id (с датированным комментарием, как делалось 21.07 и 30.07).
**Эта правка сделана ПОСЛЕ бэкап-коммита `d661929` и в него не вошла.**

**Лог Worker'а проверен** (`/head` + `/pull`, курсор не двигали): `max_id=22`, последняя
запись — `31.07 10:07:43 МСК, ASIA ВЫХОД short GER40`. За всё окно, когда были активны оба
алерта (31.07 ~22:20 → 01.08 00:39), приёмов **ноль** — рынок был закрыт. Тестовых и лишних
срабатываний нет. По EURUSD последняя запись вообще от 29.07 21:53 (`ВЫХОД short EURUSD`).

---

## Состояние деплоя

| | |
|---|---|
| Скрипт в бою | **«SMC 2-е колено v25 POI»**, `Iead0q`, pine `USER;bfab6682…`, ver 2.0 |
| Конфиг | `poiMode=snapshot / maxBos=1 / stopMode=skip / keepSetup=off / maxHoldH=42` |
| Сверка инпутов | все **32** пользовательских инпута совпали со снимком боевого v24 |
| Алерт | `5272544462`, active, webhook `bybi…workers.dev` + mobile push |
| Откат | v24 `mtV8tQ` на графике, скрыт; скрипт `USER;89bffcd5…` в аккаунте цел |

**Почему v25 — отдельный скрипт, а не новая версия v24.** У v25 пять дополнительных инпутов,
из-за чего индексы `in_32…in_36` означают совсем другое:

| Индекс | v24 (эталон вотчдога) | v25 |
|---|---|---|
| in_32 | `10000` (initial_capital) | `fvgMaxAge` |
| in_33 | `0` | `stopMode` |
| in_34 | `0` | `keepSetup` |
| in_35 | `0` | `poiMode` |
| in_36 | `false` | **`maxHoldH`** |

Поэтому `recreate-alert-with-webhook.cjs` для v25 **непригоден** — он переигрывает инпуты из
эталона и дал бы ровно те «ядовитые оверрайды», о которых предупреждает его собственная шапка.
Флаг `--pine-version` тоже не помогает: он бампает версию того же `pine_id`, а у v25 свой.
Алерт собран забором инпутов **из живой студии** — карту TradingView зафиксировал сам.

**Грабли, на которые уже наступили:** первый вариант алерта был создан с `web_hook: null`
(в `alert_list` поле вебхука не видно, а в шаблоне `create-smc-alert.cjs` стоит `null`).
Все 6 боевых потоков шлют на один Worker. Неполный алерт удалён, пересоздан корректно.

---

## Открытая развилка: A/B пола стопа 30 пипсов

Единственная гипотеза, пережившая проверку из вскрытия 64 сделок. Пипсовая шкала делит исходы
монотонно (−0.103R → +0.852R → +1.489R), разрыв тесной корзины с остальными сделками того же
окна отрицателен **во всех 6 окнах** (случайно — 1.6%). ATR-шкала делит **хуже** (разброс
0.31R против 1.59R), ветка ATR-коридора отпала.

**Цифры для решения:**

| | |
|---|---|
| Экспектанси | +0.613R → **~+1.04R** на сделку |
| Суммарный ΣR | ≈ **+2.5R за 5 лет** — в пределах шума |
| Режется входов | **38%** (24 из 64) |
| Частота | ~12.6 → **~7.6 сделок/год** |

Выигрыш здесь — в дисперсии и КПД, а не в прибыли: отсекаемая группа в агрегате около нуля,
а не глубоко убыточна. По ΣR разница утонет в шуме — показательными будут экспектанси, PF
и просадка.

⚠️ **Критерии A/B НЕ зафиксированы. Фиксируем завтра ДО старта прогона** — как делали
в прошлые разы, чтобы их нельзя было подогнать под результат.

**Заморожено:** гипотеза «глубина захода в POI» (№2/№3) — 21 сделка из 64 без восстановленной
зоны, треть выборки. Нужен надёжный столбец, то есть правка Pine с записью зоны в метку.
**В архиве:** гипотеза «среда» — 184% эффекта на двух сделках одного окна, крупнейшая выборка
сред (W1, 6 сделок) в плюсе.

---

## Что решено и закрыто в этой сессии

- **POI-трекер отклонён** — лучше снимка лишь в 3 парах из 8, проигрывает на всех сильных ячейках.
- **`keepSetup` отклонён** — экспектанси падает вдвое, просадка растёт.
- **`stopMode=scale` отклонён** — хуже `skip` на maxBos=1.
- **`maxBos` > 1 отклонён** — монотонное ухудшение подтверждено на пятилетке.
- **Тейк по ликвидности** — данные отрицательные: пул «недоехавших» 8 сделок из 64
  (максимум +8.5R/5л), а 22 сделки, доезжающие до 3R, теряют −21.7R. MFE бимодален.
- **`maxHoldH=42`** — по доходности ничья, принят как гигиена риска (снимает удержания до 41 дня).

Методическая ловушка, не повторять: формула «winrate × 3R» неприменима к ячейкам с тайм-стопом,
она завышает (давала +0.65R вместо фактических +0.576R). Считать от gross-цифр тестера.

---

## Ссылки

| Что | Где |
|---|---|
| Отчёт 5-летнего прогона матрицы | `docs/smc-v25-poi-прогон-2026-07-31.md` |
| Отчёт вскрытия 64 сделок + п.2 | `docs/smc-v25-loss-autopsy-2026-08-01.md` |
| CSV всех 64 сделок | `data/smc-v25-autopsy-64-trades.csv` |
| Сырые замеры матрицы (102 шт) | `data/smc-v25-matrix.json` |
| Исходник стратегии | `smc-2koleno-v25-poi.pine` |
| Боевые цифры v24 (ревизованы) | `docs/SMC-портфель-финал.md` |

## Состояние графика на момент закрытия

FX:EURUSD, таймфрейм **4H**, реплей выключен, **все стратегии скрыты**
(`Iead0q`, `mtV8tQ`, `556Wfi`, `KgtZ7x`). Живые алерты не трогались, кроме описанного выше.
